{
    "main": "app/main.html",                              /* APP的主入口，文件名任意；必选 */
    "name": "hostsX",                                /* APP的名称，必须具备唯一性，且符合正常变量命名；必选 */
    "description": "hosts manager",                   /* APP的简单描述 */
    "version": "1.0.0",                               /* APP的版本号 */
    "keywords": [ "hostsX","hosts", "node-webkit" ],            /* APP的关键字，搜索APP时用到 */
    "window": {                                       /* APP的窗口属性 */
        /*"icon": "link.png",                           /* APP图标（windows下，状态栏上可见） */
        "toolbar": true,                              /* 是否显示工具栏 */
        "width": 900,                                 /* 窗口初始化大小 */
        "height": 530,
        "frame": false,
        "toolbar" : true
                                         /* 是否显示外窗体，如最大化、最小化、关闭按钮 */
    }
}